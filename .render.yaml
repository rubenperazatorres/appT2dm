services:
  - type: web
    name: appT2dm
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
plan: free
region: oregon
